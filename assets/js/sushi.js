var changeBackground = function() {
  var ssContainer = document.querySelector('.ss-site > .body');
  if (ssContainer) {
    ssContainer.style.backgroundImage = "url('https://source.unsplash.com/1440x1440?sig=" + Math.random();
  }
}

